# Copy to .env.local and fill in real values. .env.local is gitignored;
# this file is not, so it stays as a template for the next person.
SUPABASE_URL=your-supabase-url-here
SUPABASE_SECRET_KEY=your-supabase-secret-role-key-here
